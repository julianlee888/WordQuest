# 🎮 Word Quest - 英文單字冒險

一個專為中小學生設計的遊戲化英文單字學習工具，以劍橋英檢分級為標準。

![Word Quest](https://img.shields.io/badge/Version-1.0.0-blue)
![License](https://img.shields.io/badge/License-MIT-green)

## ✨ 功能特色

### 學生端
- 🎯 **等級測試** - 初次登入自動測驗，決定起始等級
- 📖 **每日學習** - 每天學習 10-20 個不重複單字
- 🎮 **多種遊戲** - 配對、拼字、聽力、填空四種遊戲模式
- 📚 **智慧複習** - 間隔重複演算法，強化記憶
- 🔥 **連續天數** - 追蹤學習連續天數
- 🏆 **成就系統** - 解鎖成就徽章
- 🏅 **排行榜** - 與同學競爭
- 📊 **學習進度** - 視覺化進度追蹤
- 📤 **分享成果** - 分享學習成就

### 老師端
- 👥 **學生管理** - 查看所有學生學習進度
- 📖 **單字管理** - 新增、編輯、批次匯入單字
- 📊 **學習報表** - 班級學習統計與分析
- 📥 **匯出報表** - 下載學生學習報告

## 🏗️ 劍橋英檢分級

| 等級 | 名稱 | 適合對象 |
|------|------|----------|
| Pre A1 | Starters | 小學低年級 |
| A1 | Movers | 小學中年級 |
| A2 | Flyers | 小學高年級 |
| KET | Key | 國中程度 |
| PET | Preliminary | 國中進階/高中 |
| FCE | First | 高中進階 |

## 🚀 快速開始

### 本地測試

1. 直接用瀏覽器開啟 `index.html` 即可運行（本地模式）
2. 本地模式使用 localStorage 儲存資料

### 連接 Firebase（正式部署）

1. 前往 [Firebase Console](https://console.firebase.google.com/) 建立專案
2. 啟用 **Authentication** > **Google 登入**
3. 建立 **Firestore Database**
4. 複製設定到 `js/firebase-config.js`：

```javascript
const firebaseConfig = {
    apiKey: "你的 API Key",
    authDomain: "你的專案.firebaseapp.com",
    projectId: "你的專案 ID",
    storageBucket: "你的專案.appspot.com",
    messagingSenderId: "你的 Sender ID",
    appId: "你的 App ID"
};
```

5. 設定 Firestore 安全規則：

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
      
      match /learningLogs/{logId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
      
      match /achievements/{achievementId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
    }
    
    // 老師可以查看所有學生
    match /users/{userId} {
      allow read: if request.auth != null;
    }
  }
}
```

### 部署到 Vercel

1. 將專案推送到 GitHub
2. 連接 Vercel 並部署
3. 設定自訂網域（可選）

## 📁 專案結構

```
EnglishWord/
├── index.html          # 學生端主頁
├── teacher.html        # 老師端管理介面
├── css/
│   ├── main.css        # 主要樣式
│   ├── rpg-theme.css   # RPG 主題樣式
│   ├── animations.css  # 動畫效果
│   ├── games.css       # 遊戲樣式
│   └── teacher.css     # 老師端樣式
├── js/
│   ├── firebase-config.js  # Firebase 設定
│   ├── utils.js        # 工具函數
│   ├── auth.js         # 認證模組
│   ├── words.js        # 使用者資料管理
│   ├── games.js        # 遊戲邏輯
│   ├── achievements.js # 成就系統
│   ├── ui.js           # UI 控制
│   ├── app.js          # 主應用程式
│   └── teacher.js      # 老師端邏輯
├── data/
│   └── word-bank.js    # 單字庫
└── README.md
```

## 📚 擴充單字庫

### 方法一：線上新增
在老師端管理介面點擊「新增單字」

### 方法二：CSV 批次匯入
1. 下載 CSV 範本
2. 填寫單字資料
3. 上傳匯入

CSV 格式：
```csv
word,chinese,pos,level,example,exampleCn
apple,蘋果,n. 名詞,preA1,I eat an apple.,我吃一顆蘋果。
```

## 💰 費用說明

### 完全免費方案（推薦）
- **本地模式**：使用 localStorage，完全免費
- **Firebase 免費層級**：
  - Authentication：每月 50,000 次免費
  - Firestore：每日 50,000 讀取、20,000 寫入免費
  - Hosting：10 GB 免費

### 預估用量
| 學生數 | 每日操作 | 費用狀態 |
|--------|----------|----------|
| < 100 | ~20,000 | ✅ 免費 |
| 100-500 | ~100,000 | ⚠️ 可能接近上限 |
| > 500 | 超過免費額度 | 💵 產生費用 |

### 費用警報設定
1. 前往 [Google Cloud Console](https://console.cloud.google.com/)
2. 設定預算提醒（設為 $0）
3. 超過時會收到通知

## 🔧 自訂修改

### 修改每日學習上限
編輯 `js/ui.js` 中的 `dailyLimit` 預設值

### 新增遊戲模式
在 `js/games.js` 中新增遊戲邏輯

### 修改成就條件
編輯 `js/achievements.js` 中的 `definitions` 陣列

## 📄 授權

MIT License

## 🙏 致謝

- 單字資料參考劍橋英檢官方詞彙表
- 使用 [DiceBear](https://dicebear.com/) 產生頭像
- 使用 Web Speech API 進行發音
